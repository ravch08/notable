import { Button, Notes } from "../utils/helper";

const Home = () => {
  return (
    <main>
      <div className="container mx-auto py-8">
        <Button />
        <Notes />
      </div>
    </main>
  );
};

export default Home;
